{% extends 'base.html.twig' %}

{% block title %}Etape{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th class="link-secondary bg-primary">Id</th>
                <td class="link-secondary bg-primary">{{ etape.id }}</td>
            </tr>
            <tr>
                <th class="link-secondary bg-primary">Texte_ambiance</th>
                <td class="link-secondary bg-primary">{{ etape.texteAmbiance }}</td>
            </tr>
            <tr>
                <th class="link-secondary bg-primary">Libelle</th>
                <td class="link-secondary bg-primary">{{ etape.libelle }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-grid gap-2 d-md-flex justify-content">
    <a class="link-secondary btn btn-primary"href="{{ path('app_etape_index') }}">back to list</a>

    <a class="link-secondary btn btn-primary" href="{{ path('app_etape_edit', {'id': etape.id}) }}">edit</a>

    {{ include('etape/_delete_form.html.twig') }}
    </div>
    <h1 class="text-warning">Etape</h1>
{% endblock %}
