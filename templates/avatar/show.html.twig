{% extends 'base.html.twig' %}

{% block title %}Avatar{% endblock %}

{% block body %}
  

    <table class="table">
        <tbody>
            <tr>
                <th class="link-secondary bg-primary">Id du fichier (Avatar)</th>
                <td class="link-secondary bg-primary">{{ avatar.id }}</td>
            </tr>
            <tr>
                <th class="link-secondary bg-primary">Nom du fichier (Avatar)</th>
                <td class="link-secondary bg-primary">{{ avatar.nomFichier }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-grid gap-2 d-md-flex justify-content">
    <a class="link-secondary btn btn-primary" href="{{ path('app_avatar_index') }}">back to list</a>

    <a class="link-secondary btn btn-primary" href="{{ path('app_avatar_edit', {'id': avatar.id}) }}">edit</a>

    {{ include('avatar/_delete_form.html.twig') }}
    </div>

    <h1 class="text-warning">Avatar</h1>
{% endblock %}
