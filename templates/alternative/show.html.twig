{% extends 'base.html.twig' %}

{% block title %}Alternative{% endblock %}

{% block body %}
    <h1 class="text-warning">Alternative</h1>

    <table class="table">
        <tbody>
            <tr>
                <th class="link-secondary bg-primary">Id</th>
                <td class="link-secondary bg-primary">{{ alternative.id }}</td>
            </tr>
            <tr>
                <th class="link-secondary bg-primary">Texte_ambiance</th>
                <td class="link-secondary bg-primary">{{ alternative.texteAmbiance }}</td>
            </tr>
            <tr>
                <th class="link-secondary bg-primary">Libelle</th>
                <td class="link-secondary bg-primary">{{ alternative.libelle }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-grid gap-2 d-md-flex justify-content">
    <a class="link-secondary btn btn-primary" href="{{ path('app_alternative_index') }}">back to list</a>

    <a class="link-secondary btn btn-primary" href="{{ path('app_alternative_edit', {'id': alternative.id}) }}">edit</a>

    {{ include('alternative/_delete_form.html.twig') }}
    </div>
{% endblock %}
