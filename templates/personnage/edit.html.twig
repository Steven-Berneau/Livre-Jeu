{% extends 'base.html.twig' %}

{% block title %}Edit Personnage
{% endblock %}

{% block body %}


	{{ include('personnage/_form.html.twig', {'button_label': 'Update'}) }}
	<div class="d-grid gap-2 d-md-flex justify-content">
		<a class="link-secondary btn btn-primary" href="{{ path('app_personnage_index') }}">back to list</a>

	{{ include('personnage/_delete_form.html.twig') }}
    	</div>
	<h1 class="text-warning">Edit Personnage</h1>
{% endblock %}
