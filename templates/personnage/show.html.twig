{% extends 'base.html.twig' %}

{% block title %}Personnage{% endblock %}

{% block body %}
    

    <table class="table">
        <tbody>
            <tr>
                <th class="link-secondary bg-primary">Id</th>
                <td class="link-secondary bg-primary">{{ personnage.id }}</td>
            </tr>
            <tr>
                <th class="link-secondary bg-primary">Prenom</th>
                <td class="link-secondary bg-primary">{{ personnage.prenom }}</td>
            </tr>
            <tr>
                <th class="link-secondary bg-primary">Nom</th>
                <td class="link-secondary bg-primary">{{ personnage.nom }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-grid gap-2 d-md-flex justify-content">
    <a class="link-secondary btn btn-primary" href="{{ path('app_personnage_index') }}">back to list</a>

    <a class="link-secondary btn btn-primary" href="{{ path('app_personnage_edit', {'id': personnage.id}) }}">edit</a>

    {{ include('personnage/_delete_form.html.twig') }}
    </div>

     <h1 class="text-warning">Personnage</h1>
{% endblock %}
